- Implement flat package
- Reimplement lexer with https://pkg.go.dev/text/scanner
- Recover from errors
- Add line number and column number in errors
- bool variable declaration
- Remove lex and ast flags
- Add until=\<step=lexer, ast, codegen, codegen\> flag
- Add debug-lexer, debug-ast, debug-ast-verbose, debug-codegen

- Compile go code to binary
- Parse -run to run precompiled code
- Parse -precompile to only generate go code
- Parse -precompile-output flag to set precompiled output filename
- Parse -output flag to set output filename
- Rename v1 to transpiler